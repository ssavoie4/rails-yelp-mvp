<h1>The restaurants in near you</h1>
<% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant) %></p>
  <p><%= restaurant.category %></p>
  <p><%= restaurant.address %></p>
<% end %>
